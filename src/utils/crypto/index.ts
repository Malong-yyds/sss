// import SHA from 'jssha';

// export default function(str: string): string {
//       let shaObj = new SHA("SHA-256", "TEXT", { encoding: "UTF8" });
//       shaObj.update(str);
//       const result = shaObj.getHash("HEX");
//       return result
// }